# 8-10. Sending Messages: Start with a copy of your program from 8-9. 
# Write a function called send_messages() that prints each text message 
# and moves each message to a new list called sent_messages as it’s printed. 
# After calling the function, print both of your lists to make sure the messages were moved correctly.

# 8-11. Archived Messages: Start with your work from 8-10. Call the function send_messages() with a 
# copy of the list of messages. After calling the function, print both of your lists to show that 
# the original list has retained its messages.


chosen_messages = ['Anxiety Is Now Your Master','Microaggressions Run Amok','50 Shades of Forever']

def send_messages(sentmessage):
    sent_messages = [ ]
    for message in sentmessage:
        sent_messages.append(message)
        print(f"{message.title}")

def print_messages(messages):
    for message in messages:
        print(f"{message.title()}")

send_messages(chosen_messages)
print_messages(chosen_messages)
